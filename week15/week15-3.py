# week15-3.py 寫到一半的python版，等一下還有"寫到一半的c版"
class Solution:
    def equalSubstring(self, s: str, t: str, maxCost: int) -> int:
        N = len(s)
        for i in range(N):
            print( s[i], t[i] )

            ans = 0
            return ans
        